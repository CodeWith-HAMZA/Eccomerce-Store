import React from "react";

const AdminPanel = () => {
  React.useEffect(() => {
    scrollTo(0,0);
  }, [ ]);
  return (
    <section className="h-screen pt-20">
        admin
    </section>
  );
};

export default AdminPanel;
